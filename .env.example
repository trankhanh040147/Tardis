# Copy this file to .env and fill in real values.

# LLM / external APIs
OPENAI_API_KEY=

# Google OAuth (local files are gitignored)
GOOGLE_CREDENTIALS_FILE=credentials.json
GOOGLE_TOKEN_FILE=token.json
